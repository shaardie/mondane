# Config Environment.
# All not commented options have to be set.

# User Service Config
# Secret key for the JWT Token.
# Create one by using e.g.
# cat /dev/urandom | tr -dc 'a-zA-Z0-9' | fold -w 32 | head -n 1
MONDANE_USER_TOKEN_KEY=
#MONDANE_USER_DATABASE_DIALECT=sqlite3
#MONDANE_USER_DATABASE=./mondane.db
#MONDANE_USER_LISTEN=:8080
